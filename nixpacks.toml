[variables]
PYTHONUNBUFFERED = "1"

[phases.setup]
nixPkgs = ["python313", "uv"]

[phases.install]
dependsOn = ["setup"]
cmds = [
    "uv sync --frozen --no-dev",
]

[start]
cmd = "uv run python main.py"
